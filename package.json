{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -r dotenv/config --exec ts-node backend/index.ts",
    "start": "npx tsc && node -r dotenv/config dist/backend/index.js",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [],
  "author": "Amit Amrutiya",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.4.5",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@apollo/server": "^4.10.2",
    "@graphql-tools/merge": "^9.0.3",
    "graphql": "^16.8.1",
    "graphql-passport": "^0.6.8",
    "mongoose": "^8.3.1"
  }
}
